/**
 * Test ID        : {{ test_id }}
 * Why Generated  : {{ why_generated }}
 * Requirement    : {{ requirement_mapping }}
 * How it Exercises:
 *   GIVEN {{ given }}
 *   WHEN  {{ when }}
 *   THEN  {{ then }}
 * Coverage       : {{ coverage_contribution }}
 * Expected Outcome: {{ expected_outcome }}
 * Gaps           : {{ gaps_missing }}
 * Meaningfulness : {{ meaningfulness_check }}
 */
it('{{ test_display_name }}', async () => {
  // ── GIVEN ──────────────────────────────────────────────────────────────
  {{ given_code | default('// Set up test fixtures') }};

  // ── WHEN ───────────────────────────────────────────────────────────────
  const result = await {{ call_expression | default('functionUnderTest(input)') }};

  // ── THEN ───────────────────────────────────────────────────────────────
  {{ assertion_code | default('expect(result).toBeDefined();') }}
});
